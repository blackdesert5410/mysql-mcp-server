create a mysql mcp service with following functions：
1.defaultly login with the administrator access in this system.The default cmd is as following:
net start mysql80 
mysql -uroot -p qwer1234
you should set the username(root) password (qwer1234) as pramameters
2.create database by using the sql 
3.do crud(create read update delete and insert ..) on the databases or tables
4.more should be added as the function should be create to control the mysql.

the services should be running on this mechine or that you can use the docker tech
现在我们实现skills操作，避免在模型调用工具时产生太多token，先根据工具描述，形成工作流，再调用代码