# MySQL 连接配置
# 注意：现在主要通过命令行参数传递用户名和密码（arg1=用户名, arg2=密码）
# 环境变量仅作为备选方案，优先级：命令行参数 > 环境变量 > 默认值

# 主机地址（可选，默认: localhost）
MYSQL_HOST=localhost

# 端口（可选，默认: 3306）
MYSQL_PORT=3306

# 用户名（可选，通过命令行参数 arg1 传递，优先级更高）
MYSQL_USER=

# 密码（可选，通过命令行参数 arg2 传递，优先级更高）
MYSQL_PASSWORD=

# 默认数据库（可选）
MYSQL_DATABASE=

